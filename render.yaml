services:
  - type: web
    name: codex-ex-codespacese2606
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build && ls -la client && ls -la client/dist
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production